import { TrackPreview } from './track-preview';

describe('TrackPreview', () => {
  it('should create an instance', () => {
    const directive = new TrackPreview();
    expect(directive).toBeTruthy();
  });
});

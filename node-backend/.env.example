# --- Backend (Node.js) ---
NODE_PORT=3000
NODE_ENV=dev
NODE_JWT_SECRET=your_jwt_secret
NODE_SESSION_SECRET=your_session_secret
NODE_SHARED_SECURITY_KEY=your_shared_security_key
NODE_FEATURE_AI_SEARCH=true   # set to false to disable AI search globally

# --- Redis ---
REDIS_URL=rediss://:password@host:port          # Upstash primary (injected by Doppler)
REDIS_FALLBACK_URL=redis://127.0.0.1:6380       # Render Redis — only reachable inside Render

# --- Services Discovery ---
PYTHON_SERVICE_URL=http://localhost:8000
